<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.dia.dao.PaymentDAO">

    <insert id="insertPayment" parameterType="Payment" useGeneratedKeys="true" keyProperty="paymentId">
        INSERT INTO payment(reservation_id, method, amount, status)
        VALUES(#{reservationId}, #{method}, #{amount}, '완료')
    </insert>

    <update id="updatePaymentStatus" parameterType="Payment">
        UPDATE payment
        SET status = #{status}
        WHERE reservation_id = #{reservationId}
    </update>

    <select id="selectOne" parameterType="int" resultType="Payment">
        SELECT * FROM payment WHERE payment_id = #{paymentId}
    </select>

    <select id="checkPaymentStatus" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM payment
        WHERE reservation_id = #{reservationId}
        AND status = '완료'
    </select>
</mapper>